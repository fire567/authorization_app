import { css } from "@emotion/css";

export const AuthPage_wrapper = css({
    width: "100vw",
    height: "100vh",
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
});
